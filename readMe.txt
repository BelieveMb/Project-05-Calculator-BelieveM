Instructions
Écrans
L'entrée inférieure (peut être sélectionnée avec l'ID "value input") contient le numéro d'entrée actuel.
L'étiquette supérieure (peut être sélectionnée avec l'ID "value calcul")
contient la chaîne de calcul actuelle que l'utilisateur tape.
Les opérateurs sont précédés et suivis d'un seul espace.

Un nombre est ajouté à l'étiquette après avoir appuyé sur un opérateur.
Le calcul n'est effectué que lorsque l'on appuie sur = ou %.
Par exemple, le calcul 23 × 2 = 46 aura 2 dans l'entrée et la chaîne 23 × 2 dans
le texte au-dessus du champ d'entrée.

Boutons
Les boutons sont le SEUL MOYEN d'entrer des données et fonctionnent comme dans une calculatrice physique :

Appuyer sur les touches numériques ajoute le chiffre à la valeur d'entrée.
Le point . ne peut être présent qu'une seule fois dans une chaîne et ne peut suivre qu'un chiffre.
Le zéro ne peut pas être répété au début d'un nombre, par exemple 0000 n'est pas une valeur d'entrée valide.
Appuyer sur le bouton +/- change le signe du numéro d'entrée actuel.
Appuyer sur le bouton % retourne le résultat des calculs en pourcentage, c'est-à-dire qu'il le divise par 100.
AC réinitialise tout, et C efface uniquement l'entrée.
Lors de l'affichage du résultat d'une opération, un signe égal = est ajouté à la
chaîne d'entrée et
 le résultat est affiché dans le champ d'entrée.
Bonus
...
Ajouter un 00.


#Steps
1. Selectionner l'entrée inférieure donc la 1er entrée comme ex. 24
2. Appuyer sur l'opérateur et affiche val1 sur #calcul comme ex. 24 + 
3. Selectionner l'entrée supérieure donc la 2e entrée comme ex. 3
4. Appuyer sur = et affiche le résultat 27 sur #input et 24 + 3 = 27 sur #calcul
5. Pourcentage % affiche le résultat en pourcentage donc 27% sur #input et 24 + 3 = 27% sur #calcul

#Steps2 Boutons
1. Appuyer sur un bouton et afficher sa valeur ex. btn 1 = valeur 1
2. Le point . ne peut être présent qu'une seule fois dans une chaîne et ne peut
suivre qu'un chiffre. ex. 2.
3. à suivre 

